import 'package:flutter/material.dart';

class ScreenNoPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('페이지가 존재하지 않습니다.'),
      ),
    );
  }
}
